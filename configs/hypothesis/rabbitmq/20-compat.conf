# Temporary compatibility setting for Hypothesis workers on RabbitMQ 4.x.
# Remove after Hypothesis no longer declares transient non-exclusive queues.
deprecated_features.permit.transient_nonexcl_queues = true
