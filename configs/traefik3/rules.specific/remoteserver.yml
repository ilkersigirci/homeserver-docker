http:
  serversTransports:
    # NOTE: Proxmox redirect requests to https which causes redirect loop with traefik. This fixes it.
    proxmox-transport:
      serverName: ""
      insecureSkipVerify: true

  routers:
    webfinger-rtr:
      rule: "Host(`{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure
      middlewares:
        - middlewares-webfinger
      service: webfinger-svc

    openwrt-rtr:
      rule: "Host(`openwrt.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure
      middlewares:
        - chain-no-auth-relaxed
      service: openwrt-svc

    homeserver-rtr:
      rule: "Host(`homeserver.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure
      middlewares:
        - chain-no-auth-relaxed
      service: homeserver-svc

    nas-rtr:
      rule: "Host(`nas.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure
      middlewares:
        - chain-no-auth-relaxed
      service: nas-svc

    # wireguard-rtr:
    #   rule: "Host(`wireguard.{{env "DOMAINNAME"}}`)"
    #   entryPoints:
    #     - websecure
    #   middlewares:
    #     - chain-no-auth-relaxed
    #   service: wireguard-svc

    pocket-id-rtr:
      rule: "Host(`pocketid.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure
      middlewares:
        - chain-no-auth
      service: pocket-id-svc

    silverbullet-rtr:
      rule: "Host(`silverbullet.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure
      middlewares:
        - chain-silverbullet-tinyauth
      service: silverbullet-svc

    jellyfin-redirect-rtr:
      rule: "Host(`jellyfin.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure
      middlewares:
        - jellyfin-redirect
      service: noop@internal

    immich-public-proxy-rtr:
      rule: "Host(`share.{{env "DOMAINNAME"}}`) && PathPrefix(`/share`)"
      entryPoints:
        - websecure
      middlewares:
        - chain-no-auth-relaxed
      service: immich-public-proxy-svc

    splitpro-rtr:
      rule: "Host(`split.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure
      middlewares:
        - chain-no-auth-relaxed
        # - chain-tinyauth-relaxed
      service: splitpro-svc
  
  middlewares:
    jellyfin-redirect:
      redirectRegex:
        regex: "^https://jellyfin\\.(.+)"
        replacement: "https://jellyfin.gpu.${1}"
        permanent: true
  
  services:
    webfinger-svc:
      loadBalancer:
        servers:
          - url: "http://DUMMY"

    openwrt-svc:
      loadBalancer:
        servers:
          - url: "http://100.64.0.6"

    homeserver-svc:
      loadBalancer:
        servers:
          - url: "https://100.64.0.18:8006"
        serversTransport: proxmox-transport

    nas-svc:
      loadBalancer:
        servers:
          - url: "https://100.64.0.8:8006"
        serversTransport: proxmox-transport

    # wireguard-svc:
    #   loadBalancer:
    #     servers:
    #       - url: "http://{{env "REMOTE_SERVER_PRIVATE_IP"}}:51821" # IP of the host machine

    pocket-id-svc:
      loadBalancer:
        servers:
          - url: "http://100.64.0.19:1000"
        # healthCheck:
        #   path: "/healthz"
        #   interval: "30s"
        #   timeout: "5s"

    silverbullet-svc:
      loadBalancer:
        servers:
          - url: "http://100.64.0.19:1001"

    # jellyfin-svc:
    #   loadBalancer:
    #     servers:
    #       # - url: "http://100.64.0.19:1002"

    immich-public-proxy-svc:
      loadBalancer:
        servers:
          - url: "http://100.64.0.19:1003"

    splitpro-svc:
      loadBalancer:
        servers:
          - url: "http://100.64.0.19:1004"