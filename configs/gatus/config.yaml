# Gatus reads every YAML file in this directory because apps/gatus.yml sets
# GATUS_CONFIG_PATH=/config.
#
# Top-level sections are split by file:
# - alerting.yaml
# - endpoints.yaml
# - storage.yaml
# - ui.yaml
