services:
  tinyauth:
    image: ghcr.io/steveiliop56/tinyauth:v3
    container_name: tinyauth
    restart: $BASE_RESTART_POLICY
    profiles:
      - core
    networks:
      - t3_proxy
    security_opt:
      - no-new-privileges:true
    environment:
      APP_TITLE: Ilkerfix Auth
      APP_URL: https://tinyauth.$DOMAINNAME
      SECRET: $TINYAUTH_SECRET
      COOKIE_SECURE: "true"
      DISABLE_CONTINUE: "true"
      # USERS: $TINYAUTH_USERS
      GENERIC_CLIENT_ID: $TINYAUTH_GENERIC_CLIENT_ID
      GENERIC_CLIENT_SECRET: $TINYAUTH_GENERIC_CLIENT_SECRET
      GENERIC_AUTH_URL: https://pocketid.$DOMAINNAME/authorize
      GENERIC_TOKEN_URL: https://pocketid.$DOMAINNAME/api/oidc/token
      GENERIC_USER_URL: https://pocketid.$DOMAINNAME/api/oidc/userinfo
      GENERIC_SCOPES: "openid email profile groups"
      GENERIC_NAME: PocketID
    labels:
      - "traefik.enable=true"
      ## HTTP Routers
      - "traefik.http.routers.tinyauth-rtr.entrypoints=websecure"
      - "traefik.http.routers.tinyauth-rtr.rule=Host(`tinyauth.$DOMAINNAME`)"
      ## Middlewares
      - "traefik.http.routers.tinyauth-rtr.middlewares=chain-no-auth@file"
      ## Forward Auth
      - "traefik.http.middlewares.tinyauth.forwardauth.address=http://tinyauth:3000/api/auth/traefik"
      ####### HOMEPAGE #######
      - homepage.group=Core
      - homepage.name=tinyauth
      - homepage.icon=tinyauth.png
      - homepage.href=https://tinyauth.$DOMAINNAME
      - homepage.target=_blank
      - homepage.description=The simplest way to protect your apps with a login screen
      ####### GLANCE #######
      - glance.name=tinyauth
      - glance.icon=sh:tinyauth
      - glance.url=https://tinyauth.$DOMAINNAME
      - glance.description=The simplest way to protect your apps with a login screen