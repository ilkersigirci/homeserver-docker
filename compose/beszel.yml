services:
  beszel-agent:
    image: henrygd/beszel-agent
    container_name: beszel-agent
    restart: $MAINTENANCE_RESTART_POLICY
    # network_mode: host
    networks:
      - t3_proxy
    ports:
      - "45876:45876"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # monitor other disks / partitions by mounting a folder in /extra-filesystems
      # - /mnt/d:/extra-filesystems/sda1:ro
    environment:
      PORT: 45876
      KEY: 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICbYjtH7YvUc+kTFonrxuYC9B6UteA8ktzCsOTJWeUK1'
    profiles:
      - monitoring

  beszel:
    image: henrygd/beszel
    container_name: beszel
    restart: $MAINTENANCE_RESTART_POLICY
    networks:
      - t3_proxy
    volumes:
      - $DOCKERDIR/appdata/beszel:/beszel_data
    environment:
      TZ: "${TZ}"
    labels:
      - "traefik.enable=true"
      ## HTTP Routers
      - "traefik.http.routers.beszel-rtr.entrypoints=websecure"
      - "traefik.http.routers.beszel-rtr.rule=Host(`beszel.$DOMAINNAME`)"
      ## Middlewares
      - "traefik.http.routers.beszel-rtr.middlewares=chain-authelia@file"
      ## HTTP Services
      - "traefik.http.routers.beszel-rtr.service=beszel-svc"
      - "traefik.http.services.beszel-svc.loadbalancer.server.port=8090"
    profiles:
      - monitoring
