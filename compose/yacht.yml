services:
  # Default email:password = admin@yacht.local:pass
  yacht:
    container_name: yacht
    image: selfhostedpro/yacht:latest #devel, latest
    restart: $CORE_RESTART_POLICY
    profiles:
      - maintenance
    networks:
      - t3_proxy
      - socket_proxy
    security_opt:
      - no-new-privileges:true
    # volumes:
    #   - $DOCKERDIR/appdata/yacht:/config # Change to local directory if you want to save/transfer config locally
    environment:
      PUID: $PUID
      PGID: $PGID
      TZ: $TZ
      DISABLE_AUTH: true
    labels:
      traefik.enable: "true"
      ## HTTP Routers
      traefik.http.routers.yacht-rtr.entrypoints: "websecure"
      traefik.http.routers.yacht-rtr.rule: "Host(`yacht.$DOMAINNAME`)"
      ## Middlewares
      traefik.http.routers.yacht-rtr.middlewares: "chain-tinyauth@file"
      ## HTTP Services
      traefik.http.routers.yacht-rtr.service: "yacht-svc"
      traefik.http.services.yacht-svc.loadbalancer.server.port: "8000"
      ####### TINYAUTH #######
      tinyauth.apps.yacht.config.domain: "yacht.$DOMAINNAME"
      tinyauth.apps.yacht.ip.bypass: "$LOCAL_IPS"
      ####### HOMEPAGE #######
      homepage.group: "Maintenance"
      homepage.name: "Yacht"
      homepage.icon: "yacht.svg"
      homepage.href: "https://yacht.$DOMAINNAME"
      homepage.target: "_blank"
      homepage.description: "Docker Web Interface"
