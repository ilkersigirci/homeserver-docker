---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Renovate

on:
  push:
    branches:
      - main
    paths:
      - renovate.json
      - .github/workflows/renovate.yml
  schedule:
    - cron: "17 9-17 * * 1-5"
      timezone: "Europe/Istanbul"
  workflow_dispatch:
    inputs:
      dryRun:
        description: Dry Run
        type: boolean
        default: false
        required: true
      logLevel:
        description: Log Level
        type: choice
        default: info
        options:
          - debug
          - info
        required: true
      version:
        description: Renovate Version
        default: latest
        required: true

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  main:
    name: Renovate
    runs-on: ubuntu-latest
    steps:
      - name: Validate auth secret configuration
        shell: bash
        run: |
          if [ -z "${{ secrets.RENOVATE_TOKEN }}" ]; then
            echo "Missing Renovate auth."
            echo "Set RENOVATE_TOKEN (classic PAT with repo + workflow scopes)."
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Run Renovate
        uses: renovatebot/github-action@f3a31a786096ba6b40d0f0ffe11a494ef73bfa7c # v46.3.4
        env:
          LOG_LEVEL: ${{ inputs.logLevel || 'info' }}
          RENOVATE_AUTODISCOVER: true
          RENOVATE_AUTODISCOVER_FILTER: ${{ github.repository }}
          RENOVATE_DRY_RUN: ${{ inputs.dryRun || false }}
          RENOVATE_GIT_AUTHOR: Renovate Bot <renovate@homeserver.invalid>
          RENOVATE_INTERNAL_CHECKS_FILTER: strict
          RENOVATE_PLATFORM: github
          # PAT authentication uses Git commits with the dedicated author above.
          RENOVATE_PLATFORM_COMMIT: disabled
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
          renovate-version: ${{ inputs.version || 'latest' }}
