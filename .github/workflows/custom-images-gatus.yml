---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Build Gatus Image

on:
  push:
    branches:
      - main
    paths:
      - Dockerfiles/Gatus/**
  pull_request:
    paths:
      - Dockerfiles/Gatus/**
      - .github/workflows/custom-images-build.yml
      - .github/workflows/custom-images-gatus.yml

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
    uses: ./.github/workflows/custom-images-build.yml
    with:
      name: Gatus
      context: Dockerfiles/Gatus
      repository: homeserver-gatus
      publish: false

  publish:
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/custom-images-build.yml
    with:
      name: Gatus
      context: Dockerfiles/Gatus
      repository: homeserver-gatus
      publish: true
