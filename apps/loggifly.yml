services:
  loggifly:
    image: ghcr.io/clemcer/loggifly:v2.1.1@sha256:0b3fc3785c1918aacc19fe941cf73f47ffa828c01706441f3633cc4e038d894a
    container_name: loggifly
    restart: $RESTART_POLICY_MAINTENANCE
    profiles:
      - maintenance
    networks:
      - t3_proxy
      - socket_proxy_read
    user: $PUID:$PGID
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m,uid=$PUID,gid=$PGID,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add: []
    pids_limit: 256
    cpus: "1.0"
    mem_limit: "1G"
    ulimits:
      nofile:
        soft: 4096
        hard: 8192
    volumes:
      - $REPO_PATH/configs/loggifly:/config:ro
    environment:
      DOCKER_HOST: $DOCKER_REMOTE_HOSTS
      TZ: $TZ
      APPRISE_URL: $GOTIFY_LOGGIFLY_APPRISE_URL
      LOG_LEVEL: "INFO"
      ENABLE_HEALTHCHECK: "true"
    healthcheck:
      test: ["CMD", "python", "/app/healthcheck.py"]
      interval: 1m
      timeout: 5s
      retries: 3
      start_period: 30s
    labels:
      ####### HOMEPAGE #######
      homepage.group: "Maintenance"
      homepage.name: "loggifly"
      homepage.icon: "loggifly.png"
      homepage.target: "_blank"
      homepage.description: "Get Alerts from your Docker Container Logs"
      ####### GLANCE #######
      glance.name: "loggifly"
      glance.icon: "sh:loggifly"
      glance.description: "Get Alerts from your Docker Container Logs"
